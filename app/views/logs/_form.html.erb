<%= form_for(@log) do |f| %>
  <%= f.error_messages %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit 'Create new backlog', :class => "btn" %>
  </div>
<% end %>
